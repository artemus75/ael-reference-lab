apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: whoami
  namespace: whoami
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - whoami.k8s.example.invalid
      secretName: whoami-tls
  rules:
    - host: whoami.k8s.example.invalid
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: whoami
                port:
                  number: 80
